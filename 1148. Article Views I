import pandas as pd

def article_views(views: pd.DataFrame) -> pd.DataFrame:
    df = views[views[author_id] == views[viewer_id]]
    df = df[[author_id]].drop_duplicate().rename({'author_id':'id'})
    df = df.sortby('id')
    return df
1. 所有的column必须要加单引号
2. rename (columns = {})
3. drop_duplicates() not drop_duplicate()

import pandas as pd

def article_views(views: pd.DataFrame) -> pd.DataFrame:
    df = views[views['author_id'] == views['viewer_id']]
    df = df[['author_id']].drop_duplicates().rename(columns = {'author_id':'id'})
    df = df.sortby('id')
    return df
